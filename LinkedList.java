#Add E after B node
1) Make B node as temp node
2) Reset address to node E address
3) Point E node to C node by setting next address of E pointing to C

#AddFirst 
Let us suppose F as new node to be inserted at first
1)Make A node as temp node 
2)Make F as Head
3)Point address to A node

#AddLast
Let us suppose F as new node to be inserted at last
1) Make D node as temp node
2) Reset address of D node from null to address of F node
3) Make address of F node as NULL
